import 'package:flutter/material.dart';
class EditorShow extends StatelessWidget {
  const EditorShow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Stack(children: const [



    ],),);
  }
}
